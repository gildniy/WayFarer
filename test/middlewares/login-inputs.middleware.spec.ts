// import { validateLoginInputs } from "../../server/api/middlewares/validators";
// import { createMocks } from "../common.test";

describe('Login-inputs middleware', () => {

    // let req, res;
    //
    // beforeEach('', done => {
    //     req = httpMocks.createRequest();
    //     res = httpMocks.createResponse();
    //     done();
    // });

    context('inputs are not valid', () => {
        it('should throw 400 Invalid inputs', () => {

        })
    });
    context('inputs are valid', () => {
        it('should allow user to log in', () => {

        })
    });
});
