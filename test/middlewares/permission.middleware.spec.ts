// import { validatePermission } from "../../server/api/middlewares/validators";
// import { createMocks } from "../common.test";

describe('Permission middleware', () => {

    // let req, res;
    //
    // beforeEach('', done => {
    //     req = httpMocks.createRequest();
    //     res = httpMocks.createResponse();
    //     done();
    // });

    context('permission are not allowed', () => {
        it('should throw 403 Unauthorized action error', () => {

        })
    });
    context('permission is allowed', () => {
        it('should allow user to the action', () => {

        })
    });
});
